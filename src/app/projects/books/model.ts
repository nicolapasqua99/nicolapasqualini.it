export interface IBook {
    id: string
    isbn: string
    title: string
    author: string
    description: string
    categories: string[]
    publishedDate: string
    publisher: string
    pageCount: number
}