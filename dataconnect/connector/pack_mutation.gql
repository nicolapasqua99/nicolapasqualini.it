mutation AddPack($displayName: String, $setCode: String) @auth(expr: "auth.token.locals.role == 'admin'") {
    query {
        sets(where: { code: { eq: $setCode  } }) {
            id
        }
    }
    pack_insert(data: { displayName: $displayName, setId_expr: "uuidV4()" })
}
